---
name: zustand-best-practices.mdc
description: Best practices for state management with Zustand
globs: **/*.{ts,tsx}
---

- Create separate stores for different domains of your application.
- Use the `immer` middleware for easier state updates.
- Implement proper type safety by defining store types.
- Use the `persist` middleware for persisting state across sessions. 